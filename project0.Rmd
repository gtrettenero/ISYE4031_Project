---
title: "Project"
author: "Sebastian Bonet"
date: "2022-11-23"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

``` {r 1}
#install.packages("broom")
#install.packages("GGally")
library(tidyverse)
library(broom)
library(GGally)
input = read.csv("finalData.csv",header=TRUE)
input <- na.omit(input)
input$Duration_s <- input$Duration_ms/1000
glimpse(input)
model = lm(Popularity~. - Track_ID, data = input)
summary(model)
plot(model)
```

``` {r 2}
#Check constant variance assumption
#plot(Popularity, resid(model), main = "Residual plot")
#abline(0, 0, col = "red", lwd=3)

#Check Normality of Residuals - can also use AD test
#qqnorm(resid(model), main = "Normal plot of residuals")
#qqline(resid(model), col = "red", lwd=2)

#Transformations/dummy vars if necessary
#Check for outliers and influential points
#Correlation analysis
#Reduction of model
#Check performance of model
#create a prediction
```

